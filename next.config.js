/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'pbxt.replicate.delivery',
      'robust-sponge-49971.kv.vercel-storage.com',
      'kzcbatrvndrpersf.public.blob.vercel-storage.com'
    ],
  },
};

module.exports = nextConfig;
